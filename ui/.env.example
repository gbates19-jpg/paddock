VITE_API_WS_URL=ws://localhost:8000/events

# Dev server binds 0.0.0.0:5173 by default so it's reachable over Tailscale
# from a phone (e.g. http://<tailscale-hostname>:5173/?demo=1 needs no
# engine running at all). Override if needed:
# VITE_DEV_HOST=0.0.0.0
# VITE_DEV_PORT=5173
