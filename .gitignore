# data (gitignored per spec — historic data files, sqlite runs.db)
/data/**
!/data/.gitkeep

# historic data archives, wherever they land (e.g. a manual download
# dropped in /docs before being moved into /data/raw)
*.tar
*.bz2

# env
.env
*.pem
*.crt
*.key
certs/

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# node / vite
ui/node_modules/
ui/dist/
ui/.vite/

# os
.DS_Store
